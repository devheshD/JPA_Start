spring:
  profiles:
    active: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&autoReconnect=true
    username: root
    password: root
  jpa:
    show-sql: true        # sql 쿼리문
    database: mysql
    hibernate:
      ddl-auto: none     # 스키마 자동 생성 (create)
